# My Package

## Install

```bash
python3 tools/cli.py --root . --no-such-flag
pip install mypkg==9.9.9
```

Refer to `missing/file.py` for details.

Run `python3 tools/sideeffect.py audit` to check side effects.
