# My Package

## Install

```bash
python3 tools/cli.py --root . --out results.json
python3 tools/sideeffect.py audit
pip install mypkg==1.0.0
```

Refer to `tools/cli.py` for the CLI implementation.

All paths referenced here exist in the tree.
